# Database Configuration (MySQL - Deprecated)
DB_HOST=localhost
DB_PORT=3307
DB_USER=root
DB_PASSWORD=
DB_NAME=coursecity

# Supabase / PostgreSQL Configuration
SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key
DATABASE_URL=your_postgresql_connection_string

# Session & Security
SESSION_SECRET=your_long_random_session_secret
JWT_SECRET=your_long_random_jwt_secret

# CORS Configuration (Comma separated list of origins)
ALLOWED_ORIGINS=http://localhost,http://127.0.0.1,http://localhost:3000

# Frontend URL (Used for email links, etc.)
FRONTEND_URL=http://localhost/src/pages

# Admin Setup
ADMIN_EMAIL=admin@coursecity.com

# SMTP Configuration (Email)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM="CourseCity <info@coursecity.com>"

# OAuth Credentials (Optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=
